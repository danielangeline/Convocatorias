# Sesión 012 · 2026-09-17

**Sprint:** 2 · día 7 de 30
**Commits:** el de cierre, en la rama `sprint-2`

---

## Objetivo de la sesión

Según `ESTADO.md`, el paso 2 del Sprint 2: **Storage y adjuntos de convocatoria** (RF-07, CU-03, RNF-16, RNF-18).

El Product Owner informó al abrir que ya hizo los pendientes 3 y 8 —reenviar y cancelar una invitación, y el recorrido del catálogo del panel en el navegador— y que ambos funcionan. Quedan cerrados.

## Dos huecos de especificación, resueltos antes de programar

Los dos cambiaban el diseño, así que se preguntaron en vez de suponerlos (regla 5 de `CLAUDE.md`).

**1. ¿El bucket de adjuntos es público o privado?** `docs/04` venía de v4/v5 y marcaba privada solo la hoja de vida; eso dejaba `documentos-convocatorias` público. Pero **RN-33** (v6, sesión 006) restringe la lectura del catálogo a cuentas de empresa, y en un bucket público la ruta es adivinable: cualquiera se descargaría el TDR sin cuenta. **Decisión del Product Owner: los tres buckets privados, con URLs firmadas de 15 minutos.** Se corrigió `docs/04`; RNF-16 pasa a cubrir los tres, no solo la hoja de vida.

**2. ¿Qué admite un adjunto?** RNF-18 solo hablaba de la foto y la hoja de vida. **Decisión: PDF, Word, Excel y ZIP, hasta 20 MB** — es lo que publican las entidades (términos en PDF, formatos editables, anexos empaquetados) y 20 MB absorbe un TDR escaneado. RNF-18 se amplió.

## Qué se hizo

**Especificación primero.**

- `docs/03`: RNF-16 cubre los tres buckets; RNF-18 añade los adjuntos de convocatoria y exige que el límite y los tipos se declaren **en el bucket**, no solo en el código.
- `docs/04`: los tres buckets son privados (corrige v5) y la tabla de endpoints incorpora las cuatro rutas de adjuntos.
- `docs/05 §9.14` **nueva**: buckets, límites, rutas, políticas sobre `storage.objects`, los tres pasos de la subida, la descarga y las columnas nuevas.
- `docs/09`: CU-03 pasa a depender también de RNF-16 y RNF-18.

**Migración `20260917200000_storage_y_adjuntos`.**

- Los 3 buckets, privados, con `file_size_limit` y `allowed_mime_types`. **Ese es el enforcement real** (RNF-18): una subida directa que se pase de tamaño o de tipo la rechaza Storage sin que intervenga nuestro código.
- 12 políticas sobre `storage.objects`, una por bucket y operación (RNF-25): los adjuntos, solo administrador con `aal2`; foto y hoja de vida, solo el consultor cuyo `perfil_id` es el primer segmento de la ruta.
- `documentos_convocatoria`: columnas `tipo_mime`, `tamano_bytes` y `actualizado_at`; `storage_path` único; `nombre` entre 1 y 200 caracteres; política de `update` para el administrador y un **trigger** que impide que ese update cambie el archivo (`convocatoria_id`, `storage_path`, `tamano_bytes`, `tipo_mime`). Renombrar es cambiar el rótulo; para reemplazar el archivo se quita y se sube otro.

**Servidor (`lib/admin/documentos.ts`) y 4 endpoints** bajo `/api/admin`, que heredan el 404 del panel y la exigencia de MFA:

- `POST .../documentos/subida` valida nombre, tipo, extensión y tamaño, **decide la ruta** (`{convocatoria}/{documento}.{ext}`) y devuelve una URL firmada de subida. El nombre del archivo original no llega a la ruta: vive en la columna `nombre`.
- `POST .../documentos` registra la fila **solo si el objeto existe de verdad** y con el tamaño y el tipo que reporta Storage, no los que declaró el navegador. Además exige que ese tipo case con la extensión de la ruta.
- `PATCH .../documentos/[docId]` renombra o cambia el tipo; `DELETE` quita la fila y el objeto (CU-03 1a).
- `GET .../documentos/[docId]/enlace` devuelve una URL firmada de 15 min con el nombre descriptivo como nombre de descarga.

`conAdministrador` ahora admite varios ids de ruta (`ids`), para validar también `docId` como uuid.

**Pantalla.** La sección "Documentos" del editor dejó de ser un aviso: sube, lista con su tamaño, renombra, cambia el tipo, descarga y quita (`components/admin/DocumentosConvocatoria.tsx`). Lo que valida en el navegador —extensión y tamaño— lo repiten el servidor y el bucket, así que es solo el aviso temprano (RNF-20). `obtenerConvocatoria` devuelve los adjuntos en vez del conteo.

## Cómo se verificó

- **`scripts/prueba-adjuntos-convocatoria.mjs`** contra `npm run dev` y Supabase real, con un administrador temporal creado por la vía de invitación (TOTP verificado) y una empresa temporal. **45 comprobaciones, todas pasan**, con subida y descarga de archivos de verdad:
  - los 3 buckets existen, los 3 son privados y cada uno tiene su límite y su lista de tipos;
  - anónimo y empresa → 404 en todas las rutas de adjuntos; otro origen → 403;
  - sin nombre, tipo inválido, `.exe`, 21 MB declarados y convocatoria inexistente → 400/404;
  - subida completa: la ruta la decide el servidor, el nombre del archivo no aparece en ella, y el registro guarda el tamaño y el tipo reales;
  - registrar algo que nunca se subió → 400; **un PDF subido como `.zip` → 400 y el objeto se borra**;
  - renombrar, cambiar el tipo, renombrar a vacío → 400, documento inexistente y `docId` que no es uuid → 404;
  - cambiar `storage_path` con la sesión del administrador → rechazado por el trigger (`adjunto_inmutable`);
  - la URL firmada dura **900 s exactos** (medido sobre `exp - iat` del token), entrega el archivo y conserva el nombre descriptivo; pedir la ruta sin firmar → denegada; una cuenta de empresa **no lista ni escribe** en el bucket (RN-33);
  - la ficha y el editor traen el adjunto real;
  - quitar borra la fila **y** el objeto; quitar dos veces → 404.

  Todo lo creado se borró: cuentas, convocatoria, fuente y objetos.
- `tsc --noEmit` limpio. `next build` correcto, con las 4 rutas nuevas. `eslint .` da los mismos 2 errores ya anotados, ninguno nuevo.

**No verificado:** la pantalla en el navegador con sesión, por lo mismo de siempre —el agente no escribe contraseñas en el navegador—. La carga del editor con el adjunto real sí se comprobó por HTTP con la cookie del administrador. Queda como pendiente del Product Owner.

## Decisiones tomadas

**El archivo no pasa por el servidor.** Una función de Vercel no admite un cuerpo de 20 MB, así que va del navegador a Storage por una URL firmada. No se pierde control: la ruta la decide el servidor y la RLS del bucket exige administrador en los dos pasos. Se descartó recibir el archivo en el endpoint (se rompe a los 4,5 MB) y se descartó dejar que el navegador eligiera la ruta y subiera con su propia sesión (le daría a la pantalla una decisión que es del servidor).

**La fila se registra después de comprobar el objeto, no antes.** Y con los metadatos que reporta Storage. Si el navegador declarara el tamaño, bastaría con mentir para saltarse RNF-18 en la fila, aunque el bucket sí lo hubiera aplicado.

**Un objeto sin fila es invisible, y se acepta como el modo de fallar.** Si la subida ocurre y el registro no, queda un objeto suelto: nadie puede descargarlo, porque toda descarga parte de la fila. La pantalla pide borrarlo cuando el registro falla. Se prefirió eso a registrar primero y arriesgar una fila que apunta a un archivo que no llegó.

**La descarga la autoriza la RLS, no la ruta.** El endpoint lee la fila con la sesión de quien pide y solo firma si puede verla. Así el mismo endpoint servirá a la empresa en el paso 4 sin cambiar ninguna regla.

**Los adjuntos sí se borran**, a diferencia de fuentes y categorías (RN-07): un adjunto equivocado no es historia que valga la pena conservar, y CU-03 1a lo pide.

**Ni renombrar ni quitar dependen del estado de la convocatoria.** CU-03 1a nombra el caso de antes de publicar, pero no prohíbe corregir un adjunto después. Se siguió el criterio de `guardar_convocatoria`, que deja editar una publicada, en vez de inventar una restricción que el documento no declara.

## Segunda parte: tres cosas resueltas al cierre

**1. El ensayo de migraciones deja de depender de la memoria.** El hallazgo se convirtió en regla: `docs/10 §17.3` añade el paso 6 del ritual, con el procedimiento escrito y la advertencia de que el `raise` va **dentro** del archivo que se ensaya.

**2. El bloque que `next dev` escribe en `CLAUDE.md`.** Al arrancar el servidor de desarrollo, Next.js añade solo un bloque `nextjs-agent-rules` al final de `CLAUDE.md`. No lo escribió el Product Owner ni el agente. Se dejó fuera del primer commit para que lo decidiera él; **autorizó guardarlo**, porque el programa lo vuelve a poner en cada arranque y tenerlo sin guardar deja al proyecto con un cambio pendiente que nadie hizo.

**3. Publicar ya no exige adjuntos — RN-01, RF-09 y CU-05 modificados.** Al preparar el paso 3 se leyó con cuidado RN-01 y resultó que **no era una decisión abierta**: desde v5 la regla ya exigía ≥1 documento adjunto para publicar, y quien se había quedado corta era la implementación (`guardar_convocatoria` solo comprueba enlace y requisitos, porque se escribió cuando los adjuntos no existían). Planteado así al Product Owner, **decidió lo contrario: se puede publicar sin términos de referencia.** Razón: una entidad a veces anuncia la convocatoria antes de colgar los documentos, y bloquear la publicación por eso le quita días a la empresa mientras el cierre corre; el enlace oficial siempre lleva al portal de la entidad, donde los documentos sí están.

Consecuencias, ya escritas:
- `docs/03` RN-01, `docs/02` RF-09 y `docs/01` CU-05 pierden la exigencia de adjunto, marcadas *(mod. v6, sesión 012)*;
- CU-05 gana el alterno **3d**: publicar sin ningún adjunto se permite, pero **se advierte antes de confirmar**, porque la generación con IA pierde el texto del TDR como contexto (CU-33 paso 4);
- lo que `guardar_convocatoria` valida hoy pasa a ser **exactamente** la regla: no hay que ampliarla en el paso 3, solo escribir `publicar`/`despublicar` y la advertencia.

## Qué quedó abierto

- Sprint 2, paso 3: publicación validada en servidor (RF-09), con la advertencia de CU-05 3d.
- Los archivos del perfil del consultor (foto y hoja de vida): los buckets y sus políticas ya están; falta la pantalla y sus endpoints, que van con el módulo de consultores.
- Recorrer la sección de documentos en el navegador (pendiente del Product Owner).

## Hallazgos no planificados

- **El ensayo de migraciones con `raise` en un archivo aparte no ensaya nada.** `supabase db push` corre **cada** migración en su propia transacción: un archivo de ensayo posterior que se revienta a propósito solo se revierte a sí mismo, y la migración real que iba antes ya quedó comprometida. Aquí no hubo daño —el SQL era correcto y es el que se quería aplicar—, pero el método solo sirve si el `raise` va **dentro del mismo archivo** que se está ensayando. Anotado en `ESTADO.md`.
- `set role supabase_storage_admin` está negado al rol que corre las migraciones (42501), pero ese mismo rol **sí** puede crear políticas sobre `storage.objects` directamente.
- El reloj de esta máquina, ~141 s atrasado (hallazgo de la sesión 009), hace fallar cualquier medición de caducidad hecha contra `Date.now()`. La prueba mide `exp - iat` dentro del token.
- **RNF-06 y RNF-18 se contradicen**: el primero admite "carga admin hasta 50 MB" y el segundo fija 20 MB por adjunto, que es lo que el bucket hace cumplir. Anotado en `ESTADO.md`; hay que dejar un solo número.

## Estado al cerrar

- `ESTADO.md` actualizado: sí
- `docs/11-avance-por-requerimiento.md` actualizado: sí. RF-07 pasa a `servidor`; RNF-16 y RNF-18 pasan de `pendiente` a `servidor`, con la parte del consultor anotada como pendiente
- `tsc --noEmit` y `lint`: `tsc` limpio; `lint` con los 2 errores previos ya anotados
- Siguiente paso: Sprint 2, paso 3 — publicación validada en servidor (RF-09, RN-01, RNF-29), **sin exigir adjuntos** y con la advertencia de CU-05 3d
- Decisiones abiertas: ninguna nueva. La de "¿publicar exige adjunto?" nació y se cerró en esta sesión
